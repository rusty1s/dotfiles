#!/bin/bash

if [ ! -d "$HOME/svn" ]; then
  echo "Abort: No svn repository found in $HOME/svn."
  exit 0
fi

mkdir -p "$HOME/svn/ftl-symlinks"

for file in $(find "$HOME/svn" -path "*/ftl-admin/*.ftl"); do
  ln -sf "$file" "$HOME/svn/ftl-symlinks/$(basename $file)"
done
